import Typography from "typography"

const typography = new Typography({
    baseFontSize: "24px",
    headerFontFamily: ["Arial"],
    bodyFontFamily: ["Lora, Arial"],
});

export const { scale, rhythm, options } = typography
export default typography